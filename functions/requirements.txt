# Python dependencies bundled into member and admin Lambda ZIPs.
# Installed at build time via:  pip install -r functions/requirements.txt -t <deps_dir>
# cryptography is listed explicitly because pip's --platform flag does not resolve
# PEP 508 extras (e.g. PyJWT[cryptography]) reliably for cross-platform installs.
PyJWT>=2.9
cryptography
